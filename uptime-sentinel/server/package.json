{
  "name": "uptime-sentinel-server",
  "version": "1.0.0",
  "description": "Backend API and worker service for Project Uptime Sentinel",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "worker": "node ping-worker.js",
    "postbuild": "cd ../client && npm install && npm run build",
    "production": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "uptime",
    "monitoring",
    "puppeteer",
    "website"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.6",
    "puppeteer": "^24.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
